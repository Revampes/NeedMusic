Changes required 

1. change the background to be glass (semi-transparent) instead of deep blue style (or maybe add settings so that user can swap between different styles)
2. the volume still does not save configs
3. the folder path should also save configs
4. add some transition animations between pages/tabs
5. also i would like to add settings of Statup and window behaviour which open this app automatically after user log into the computer and something like close button should minimize the application window instead of closing it