import React from "react";
import ReactDOM from "react-dom/client";
import DynamicIslandWindow from "@ui/components/DynamicIslandWindow";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <DynamicIslandWindow />
  </React.StrictMode>
);
